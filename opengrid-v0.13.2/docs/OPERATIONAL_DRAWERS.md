# Operational drawers

The map header exposes two searchable lists:

- **Contacts**: TRACK records such as AIS, ADS-B, fused tracks, and detections.
- **Assets**: ASSET and SENSOR records that OpenGrid can monitor or task.

Both the left list and right inspector collapse independently. Plugins and Artifacts open in new tabs so the operational map remains open.
