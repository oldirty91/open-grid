# Local ENC data

Place GeoJSON FeatureCollections here for offline/reference use. v0.13.2 mounts this directory read-only into the NOAA ENC adapter at `/data/enc`. Remote NOAA discovery remains available while the local importer is finalized.
