# RIDOT fixed-camera adapter

Publishes a stationary camera Entity and supports snapshot/recording prototype Tasks. The bundled default is deterministic demo imagery; set a native camera source in the adapter configuration when available.
